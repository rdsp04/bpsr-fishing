pyinstaller:
  entry_script: main.py
  onefile: true
  windowed: true
  add_data:
    - "images;images"

nsis:
  script: "installer.nsi"
  output: "bpsr-fishing-Setup.exe"
  install_dir: "$PROGRAMFILES\\bpsr-fishing"
